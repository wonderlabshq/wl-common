language: node_js

node_js:
  - "7"

sudo: false

cache:
  yarn: true
  directories:
    - ".eslintcache"
    - "node_modules"
install:
  - npm install -g codecov

script:
  - yarn run test:coverage
  - codecov
